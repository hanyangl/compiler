// This example is thanks to TypeScript Handbook

interface LabeledValue {
  label: string;
}

func printLabel(
  labeledObj: LabeledValue,
) {
  print(labeledObj->label);
}

let myObj = {
  size: 10,
  label: 'Size 10 Object',
};

printLabel(myObj);
