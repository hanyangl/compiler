const data = {
  name: 'Sflyn',
  year: 2020,
};

for ([key, value] of data) {
  let type: string = "";

  if (value is number) {
    type = "a number";
  } else if (value is string) {
    type = "a string";
  }

  print("`" + key + "` value is " + type);
}
